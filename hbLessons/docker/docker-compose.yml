version: '3.2'

volumes:
  hbLessons-app-postgres:
    driver: local

services:
  postgres:
    image: postgres:9.6
    container_name: hbLessons-app-postgres
    volumes:
      - type: volume
        source: hbLessons-app-postgres
        target: /var/lib/postgresql/dataConfig
    environment:
      POSTGRES_DB: hbLessons-app
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespass
    ports:
      - "5432:5432"

# jdbc:postgresql://localhost:5432/hbLessons-app